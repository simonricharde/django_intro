[tool.poetry]
name = "candy_site"
version = "0.1.0"
description = ""
authors = ["seswaram <seswaram@amazon.com>"]

[tool.poetry.dependencies]
python = "^3.7.7"
django = "^3.0.6"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
